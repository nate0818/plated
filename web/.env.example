# The waitlist route handler writes to Supabase with the service role, which
# bypasses RLS. This key never reaches the browser: it is read only inside
# app/api/waitlist/route.ts on the server. Set both in Vercel's project
# environment for Production and Preview; copy this file to .env.local for dev.
SUPABASE_URL=https://dyvrksooelbkzkprqlhk.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
