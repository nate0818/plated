# Xcode
build/
DerivedData/
.verify-dd/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# macOS
.DS_Store

# Secrets / local config
*.local.xcconfig
secrets.plist

# The tour photograph originals. Kept out of the repo (about 12MB) but
# kept ON DISK so a crop can be retuned without fetching them again.
tour-photos/
